export enum DownloadType {
  document = 'document',
  signedDocument = 'signedDocument',
  agentDocument = 'agentDocument',
  agentCV = 'agentCV',
  agentFormation = 'agentFormation',
}

export enum ReturnFileType {
  path = 'path',
  fileName = 'baseName',
  fileNameWithoutExtension = 'baseNameWithoutExtension',
  directory = 'directory',
}
